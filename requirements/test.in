-c app.txt

pytest
pytest-flask
pytest-sugar
pytest-cov
# to test calls to the API
requests
# let's tests run successfully in containers
fakeredis
# required with fakeredis, maybe because we use rq
lupa